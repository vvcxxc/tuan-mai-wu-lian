// 此样式影响poster文件内 多个海报组件
.poster {
  position:fixed;
  top:0;
  left:0;
  right :0;
  bottom:0;
  z-index: 100;
  height 100vh;
  width 100vw;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background-color rgba(0,0,0,0.6)
  .save-img {
    margin-top 43px;
    display: flex;
    justify-content: center;
    align-items: center;
    width:472px;
    height:95px;
    font-size:30px;
    font-family:PingFang SC;
    color:rgba(255,255,255,1);
    background:rgba(255,68,68,1);
    border-radius:48px;
  }
}